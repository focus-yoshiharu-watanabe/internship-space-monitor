"""
2人で一緒に書くファイル（ペアプロ）

統合したあとは変更しないこと。
2人のルール（capacity.py と absence.py）が、どちらもこの関数に頼っているため。
"""


def count_people(detections, zone):
    """
    ゾーンの中にいる人の数を返す。

    detections: 検出結果のリスト。1つ1つはこんな形の辞書
        {"class": "person", "x1": 120, "y1": 80, "x2": 260, "y2": 400, "conf": 0.87}
        (x1, y1) が枠の左上、(x2, y2) が枠の右下。単位はピクセル
        x は右に行くほど大きく、y は「下」に行くほど大きい

    zone: ゾーンの矩形 (x1, y1, x2, y2)。形は detections と同じ考え方

    2人で決めること：人の位置は、枠の「中心」で見る？ それとも「足元」で見る？
    """
    # TODO: ここを2人で書く
    return 0
